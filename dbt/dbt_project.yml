name: 'lol_analytics'
version: '1.0.0'
profile: 'lol_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  lol_analytics:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
