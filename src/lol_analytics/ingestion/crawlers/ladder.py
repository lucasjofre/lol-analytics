"""Ladder crawler: walks the ranked ladder to discover players worth tracking.

`main()` is the wheel entry point used by the Databricks ingestion job; the same
logic is reachable locally via `lol-analytics ingest ladder`.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError
